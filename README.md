# 🃏 Poker Range Trainer

Веб-приложение для создания и тренировки покерных диапазонов. Позволяет визуально редактировать диапазоны рук, организовывать их в папки и тренироваться на запоминание.

## 🚀 Возможности

- **Редактор диапазонов**: Создавайте и редактируйте покерные диапазоны с помощью интуитивной матрицы рук
- **Тренажер**: Практикуйтесь в запоминании диапазонов
- **Организация**: Создавайте папки и структурируйте свои диапазоны
- **Сохранение в облаке**: Все данные хранятся в Supabase и синхронизируются между устройствами

## 🛠️ Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка и разработка
- **Tailwind CSS** - современные стили
- **Supabase** - база данных и бэкенд
- **Lucide React** - иконки

## 📋 Требования

- Node.js 18+ и npm
- Аккаунт Supabase (бесплатный план доступен)

## 🔧 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/poker-range-trainer.git
cd poker-range-trainer
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка Supabase

1. Создайте проект на [Supabase](https://supabase.com)
2. Перейдите в **Settings** → **API**
3. Скопируйте **Project URL** и **anon/public key**
4. Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

5. Откройте `.env` и вставьте ваши ключи:

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

### 4. Настройка базы данных

1. В Supabase перейдите в **SQL Editor**
2. Создайте новый запрос
3. Скопируйте и выполните содержимое файла:
   - `supabase/migrations/20251017113341_create_poker_ranges_tables.sql`
   - `supabase/migrations/20260131093257_fix_rls_policies_for_ranges.sql`

### 5. Запуск проекта

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

Приложение будет доступно по адресу `http://localhost:5173`

## 📦 Деплой

### Vercel (рекомендуется)

1. Загрузите код в GitHub
2. Зайдите на [Vercel](https://vercel.com)
3. Импортируйте ваш GitHub репозиторий
4. Добавьте переменные окружения:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
5. Нажмите Deploy

### Netlify

1. Загрузите код в GitHub
2. Зайдите на [Netlify](https://netlify.com)
3. Добавьте новый сайт из Git
4. Выберите ваш репозиторий
5. Настройки сборки:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. Добавьте переменные окружения в настройках сайта
7. Нажмите Deploy

## 📁 Структура проекта

```
project/
├── src/
│   ├── components/      # React компоненты
│   ├── lib/            # Конфигурация Supabase
│   ├── types/          # TypeScript типы
│   ├── utils/          # Утилиты и функции
│   ├── App.tsx         # Главный компонент
│   └── main.tsx        # Точка входа
├── supabase/
│   └── migrations/     # SQL миграции для базы данных
├── .env.example        # Шаблон переменных окружения
└── package.json        # Зависимости проекта
```

## 🔒 Безопасность

- Файл `.env` с реальными ключами **не должен** попадать в Git (уже в `.gitignore`)
- Используйте `anon/public` ключ Supabase (безопасен для фронтенда)
- Row Level Security (RLS) настроен в базе данных

## 📝 Лицензия

Этот проект создан для личного использования.

## 🤝 Вклад

Если вы хотите улучшить проект, создайте Issue или Pull Request!

---

Создано с ❤️ для покерных энтузиастов
